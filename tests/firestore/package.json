{
  "name": "roomie-firestore-rules-tests",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "firebase emulators:exec --project demo-roomie-rules --only firestore,storage --config ../../firebase.rules-test.json 'node --test --test-concurrency=1 *.test.mjs'"
  },
  "devDependencies": {
    "@firebase/rules-unit-testing": "5.0.2",
    "firebase": "12.19.0",
    "firebase-tools": "15.30.0"
  }
}
